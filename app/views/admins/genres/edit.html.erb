<h1 class ="title">ジャンル編集</h1>

<%= form_with model:@genre, url:"/admins/genres/#{@genre.id}", local:true do |f| %>

<p class="admins_genre_letter">ジャンル名</p>
<div class="admins_genre_file"><%= f.text_field :name %></div>
<div class="admins_genre_register"><%= f.submit '変更を保存' %></div>
<% end %>
